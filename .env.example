## Archivo de ejemplo de variables de entorno para desarrollo/producción
# Copia este archivo a `.env` y ajustar valores.

# Django
DJANGO_SECRET_KEY=replace-me-with-secure-key
DJANGO_DEBUG=True

# Database (Postgres example)
POSTGRES_DB=cobramax_db
POSTGRES_USER=cobramax_user
POSTGRES_PASSWORD=password123
POSTGRES_HOST=db
POSTGRES_PORT=5432

# Email / SMTP (opcional)
DJANGO_EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
EMAIL_HOST=smtp.example.com
EMAIL_PORT=587
EMAIL_HOST_USER=you@example.com
EMAIL_HOST_PASSWORD=supersecret
EMAIL_USE_TLS=True
DEFAULT_FROM_EMAIL=noreply@cobramax.local

# Twilio (opcional)
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_WHATSAPP_NUMBER=whatsapp:+1415XXXXXXX

# Celery / Redis
CELERY_BROKER_URL=redis://redis:6379/0
CELERY_RESULT_BACKEND=redis://redis:6379/1
# Ejemplo de variables de entorno para desarrollo
# Twilio (WhatsApp)
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_WHATSAPP_NUMBER=+1XXXXXXXXXX

# Email (SMTP)
EMAIL_HOST=localhost
EMAIL_PORT=25
EMAIL_HOST_USER=
EMAIL_HOST_PASSWORD=
EMAIL_USE_TLS=False

# Celery/Redis
CELERY_BROKER_URL=redis://localhost:6379/0
CELERY_RESULT_BACKEND=redis://localhost:6379/1

# Django
DJANGO_SECRET_KEY=changeme
DJANGO_DEBUG=True
